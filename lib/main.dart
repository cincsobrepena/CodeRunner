import 'package:flutter/material.dart';
import 'package:iosappdevelopment_finals_tatel_sobrepena_tolentino/language_select_screen.dart';
import 'level_select_screen.dart';

void main() => runApp(CodeSnippetApp());

class CodeSnippetApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: StartScreen(),
    );
  }
}
